export interface profileInterface {
  avatar_url: string,
  name: string,
  bio: string,
  location: string,
  html_url: string,
  login: string
}

export interface reposInterface {
  name: string,
  stargazers_count: number,
  description: string,
  url: string
}